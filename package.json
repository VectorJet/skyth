{
  "name": "skyth",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "start": "bun run skyth/cli/main.ts",
    "build:bin": "mkdir -p dist && bun build --compile --minify --sourcemap --outfile dist/skyth skyth/cli/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.20",
    "@ai-sdk/openai": "^2.0.61",
    "@ai-sdk/openai-compatible": "^1.0.17",
    "ai": "^6.0.97",
    "cron-parser": "^5.5.0",
    "yaml": "^2.8.2",
    "zod": "^4.3.6"
  }
}
