# Questions for User - Phase 2

**Date:** 2026-02-13

---

## OpenClaw Migration Questions

## Question 1: Migration Strategy

**Context:** We need to migrate your OpenClaw data (~/.openclaw/) to Skyth.

**Question:** Should migration happen:

1. Automatically on first Skyth run (if OpenClaw detected)
2. On-demand via `skyth migrate openclaw` command
3. Ask user interactively

---

## Question 2: Agent Identity

**Context:** When agent migrates from OpenClaw to Skyth, it should understand its new "body".

**Question:** What should the agent call itself?

1. Keep same name (from ~/.openclaw/agents/main/)
2. Rename to "Skyth Agent"
3. Ask user

---

## Question 3: Data Retention

**Context:** OpenClaw has sessions, credentials, extensions, etc.

**Question:** Should we copy ALL data or ask per-category?

1. Copy all automatically
2. Ask per-category (sessions, credentials, channels, extensions)
3. Only copy essential (sessions, memories)

---

## Question 4: Legacy OpenClaw

**Question:** After migration, should we:

1. Leave ~/.openclaw/ as-is
2. Offer to delete ~/.openclaw/ after confirmation
3. Move to ~/.openclaw.bak/

---

## Agent Architecture Questions

## Question 5: Agent Directory Location

**Context:** The old Python Skyth used `backend/agents/{name}/` for agent directories. The current TypeScript uses `core/backend/agents/` but has definitions in code.

**Question:** Where should agent manifest directories live?

**Options:**

1. `core/backend/agents/{name}/` - Keep in backend (current location)
2. `~/.skyth/agents/{name}/` - User-installed agents only
3. Both: built-in in `core/backend/agents/`, user in `~/.skyth/agents/`
