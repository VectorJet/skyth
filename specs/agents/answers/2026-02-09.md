
The Quasar and epsilon project is still in rust the specifications are in python because it's easy to write and read I hope that clears things up. we are not inventing the wheel.

if you want create a prototype implementation with this custom architecture as mentioned below.

First create a epsilon directory in quasar and initialize git there.

then configure a rust project.

the architecture is as follows.

epsilon is separate from quasar but inherits components from it only 3 event types exist as of now. 

let's consider a tick as a set amount of time 

epsilon init . will initialize the hidden folder .epsilon in the current directory with a 0th quasar event this would create a very compressed copy of all the files in the folder each tick counts as a commit.

clone the reference [jj](https://github.com/jj-vcs/jj.git)

in epsilon/refs in quasar directory.

we will use it as a reference as it's written in rust too or we can build on top of that as it's fairly mature 

if we build on top of jj(recommend) we need to add the discussed features in jj 

---

               End of document