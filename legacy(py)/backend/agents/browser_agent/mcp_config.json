{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp",
        "--executable-path",
        "/usr/bin/chromium",
        "--headless",
        "--isolated",
        "--no-sandbox"
      ]
    }
  }
}